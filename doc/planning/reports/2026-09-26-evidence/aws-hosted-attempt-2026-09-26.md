# AWS and hosted Demo attempt — 26 September 2026

Checked at approximately 11:08 UTC against `origin/main` commit `df2de03dbc5211c281be5034fde8be04c8718fa7` (merged PR #224). **No AWS resource was created, changed, started, or deleted. There is no deployed HTTPS endpoint yet.**

## Release and security evidence

- Main [Quality checks](https://github.com/Collaboration95/rewind-app/actions/runs/36237307651) and [CodeQL](https://github.com/Collaboration95/rewind-app/actions/runs/36237307652) succeeded on that exact commit.
- Built a release bundle from a clean, detached checkout of that commit using `python3 deploy/release.py build --green-sha df2de03dbc5211c281be5034fde8be04c8718fa7 --config-version demo-v1`. `deploy/release.py verify` confirmed the same commit. Local bundle: `/private/tmp/rewind-df2de03dbc5211c281be5034fde8be04c8718fa7.tar` (324 MB); SHA-256 `1ce1aecef6cab8e040c9c0ec48fdc827dedf03edba5196f40b70fd39d5fed452`. This `/private/tmp` file is local and ephemeral, not a published release artifact.
- Manually dispatched [scan-only release-security run](https://github.com/Collaboration95/rewind-app/actions/runs/36237806296) on the same main commit. Runtime image, web image, and IaC scans all succeeded. Retained artifact digests: runtime `sha256:e4178448a93968d041fe2ce5059f0a0b077479c4076d38e97e840a18cedc25b8`, web `sha256:168229b46657467109c53a5d443ef1d2cf0d0c3deea7d3398cc3cb254fa55c6e`, IaC `sha256:608460be90ae79aa4976e0463b8c75db107ad5128520fa133bfb3c344048b6c2`.
- `terraform fmt -check -recursive infra/terraform` and `terraform -chdir=infra/terraform/demo validate` succeeded on the locally prepared configuration. The local ignored `terraform.tfvars` proposes `public_https_distribution_enabled=true`, an allowlist entry for `rewind-demo-web -> rewind-demo`, and expected instance state `running`. It has **not** been applied or committed. The merged-main HTTPS origin/policy tests passed, 15/15.
- Main has zero open Dependabot alerts and zero open CodeQL alerts. Three production findings resolved through #224. The remaining `js/xss-through-dom` [alert #3](https://github.com/Collaboration95/rewind-app/security/code-scanning/3) was dismissed as a documented false positive: `video.src` receives a locally generated `blob:` URL from a selected `File`, the function rejects non-`blob:` schemes, and a regression rejects `javascript:` while accepting `blob:`. CodeQL did not model that guard. The dismissal must be revisited if the source or guard changes.

## Live checks and reviewable plan

- `AWS_PROFILE=rewind-coding-agent` identified the expected account. Lightsail inventory showed no Demo instance or static IP in `ap-southeast-1`, and no distribution through the required `us-east-1` distribution API. Therefore there is no hostname to probe for #200 or run hosted journeys against for #145.
- AWS's bundle APIs report `micro_3_0` at **US$7/month** and `small_1_0` distribution at **US$2.50/month**: approximately **US$9.50/month** together if retained for a whole month, before S3/logging/transfer overages. Actual-cost alerts at US$10 and US$15 are notifications, not a spending cap. Transfer overage has no fixed maximum here.
- S3 `ListObjectsV2` showed a candidate `rewind-20260921T060633Z` manifest (345 bytes), database archive (7,874 bytes), and media archive (110 bytes). `s3:GetObject` of the manifest returned `AccessDenied` for `rewind-coding-agent`; that role also received S3 state `HeadObject` HTTP 403. A first wake dry run under that profile stopped at recovery selection. Those **permission errors did not prove the backup invalid**.
- Under the designated `rewind-terraform-apply` profile, the **actual** `wake-demo.sh --latest --dry-run` completed. It selected `s3://rewind-demo-backups-330599756236/rewind-demo/rewind-20260921T060633Z.manifest.json`, downloaded the manifest and both archives, verified manifest/schema/remote sizes/archive checksums, and passed its Terraform plan guard. It reported `5 to add, 3 to change, 0 to destroy` and explicitly `No Terraform apply, SSH, SCP, or rsync was performed`. This validates the stored backup files, though full application restorability can only be established after an authorized restore and health check.
- An independently saved fresh remote-state plan with the same proposed variables is kept locally at `/private/tmp/rewind-https-df2de03.tfplan` (SHA-256 `f0a5cfb46172cff03ba5424c2bbeed57f096afb4e9cc77563661d4f51ee8c1fb`) with private JSON/log alongside it. **Do not commit or share the raw plan/state.** Its five creates are `aws_lightsail_instance.rewind[0]`, `aws_lightsail_instance_public_ports.rewind[0]`, `aws_lightsail_static_ip.rewind[0]`, `aws_lightsail_static_ip_attachment.rewind[0]`, and `aws_lightsail_distribution.web[0]`. Its three updates are `aws_iam_role_policy.cost_safety_audit_scheduler`, `aws_iam_role_policy.power_controller[0]`, and `aws_lambda_function.cost_safety_audit`. There are zero destroys and 27 managed no-ops. The exact `validate_recreate_plan` function from `wake-demo.sh` accepted this saved JSON. The IAM policy fields are computed until apply; the Lambda update changes source-code hash/last-modified metadata. These updates need Andrew's review, not merely a count check.
- Terraform's drift section marks the same five Demo resource addresses as deleted outside Terraform, consistent with issue #190's prior direct deletion. The proposed creates restore exactly those addresses. The planned instance is Ubuntu 24.04 `micro_3_0` in `ap-southeast-1a`; HTTP port 80 is public for the distribution origin, while SSH is limited to the configured `/32` plus Lightsail Connect. The `small_1_0` distribution targets `rewind-demo` in `ap-southeast-1` over HTTP and serves an HTTPS viewer hostname.
- The historical direct deletion made remote-state reconciliation necessary; the fresh plan now reflects absent resources and proposes recreation, rather than blindly reusing the old 5-create/1-update summary. No AWS resource has been mutated by this inspection or dry run.

## Exact remaining sequence

1. Andrew reviews the **fresh exact plan** and the private HTTPS/audit variable values, with special attention to the two IAM policy updates, Lambda update, expected instance state, and recovery candidate. Reject unexpected changes. The [AWS operations skill](/Users/speedpowermac/.codex/skills/rewind-aws-operations/SKILL.md) reserves Terraform applies for human-reviewed work.
2. After that review, Guruprasath runs the guarded `wake-demo.sh --latest --apply --confirm` with the exact bundle digest above and approved configuration. The apply invocation generates a new plan; confirm it is equivalent to the reviewed plan before continuing. Record the applied plan, restored release ID, AWS inventory and distribution hostname. Do not use `--seed` in place of the verified historical recovery point.
3. Verify `https://<hostname>/` and `/api/health`, then perform two clean hosted journeys, restart/persistence and rollback/fallback checks, and a non-author hosted acceptance run. Do not close #190, #200, #145, or tracker #203 without those respective results.

The dirty original checkout and its private configuration were not modified. The proposed Terraform variables exist only in an ignored file in the isolated `/private/tmp/rewind-main-verify` worktree.
