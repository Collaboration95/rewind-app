# Review request — Demo wake and HTTPS origin

Please review the fresh Terraform plan for main `df2de03dbc5211c281be5034fde8be04c8718fa7` before any apply. The private local plan is `/private/tmp/rewind-https-df2de03.tfplan`, SHA-256 `f0a5cfb46172cff03ba5424c2bbeed57f096afb4e9cc77563661d4f51ee8c1fb`. Inspect it on the trusted operator machine with `terraform -chdir=infra/terraform/demo show -no-color /private/tmp/rewind-https-df2de03.tfplan`; do not commit or paste raw plan/state or private variables. The [sanitized evidence note](2026-09-26-evidence/aws-hosted-attempt-2026-09-26.md) records the full resource list and checks.

The plan is **5 creates, 3 updates, 0 destroys**. Terraform reports drift deletions for those same five Demo addresses following the earlier direct AWS deletion. Creates are the `micro_3_0` Ubuntu 24.04 Demo instance in `ap-southeast-1a`, public ports (HTTP 80 globally; SSH restricted to the configured `/32` and Lightsail Connect), static IP, attachment, and `small_1_0` HTTPS distribution with the Demo instance as its HTTP origin. Updates are the cost-safety audit scheduler IAM policy, power controller IAM policy, and cost-safety audit Lambda. The exact wake-script allowlist accepted the plan. Check that the two policies remain least-privileged, the Lambda stays report-only, the expected instance state is `running`, and the distribution allowlist maps `rewind-demo-web` to `rewind-demo`. Reject any extra resource or surprise replacement.

The guarded `wake-demo.sh --latest --dry-run` downloaded and checksum-verified the 21 September manifest/database/media recovery point. The release bundle from green main is `/private/tmp/rewind-df2de03dbc5211c281be5034fde8be04c8718fa7.tar`, SHA-256 `1ce1aecef6cab8e040c9c0ec48fdc827dedf03edba5196f40b70fd39d5fed452`. The instance and distribution are absent today. AWS prices the configured bundles at about US$9.50/month together before usage charges; US$10/$15 alerts are notifications, not a hard cap.

If the exact plan and cost are acceptable, record your approval and have Guruprasath run the guarded apply/restore. That script creates a fresh plan, so compare it with the reviewed resource changes before applying. Once healthy, record the actual distribution hostname, HTTPS `/` and `/api/health` responses, two hosted journeys, restart/persistence and rollback/fallback results, and a non-author acceptance run. Do not mark #190, #200, or #145 done before their evidence exists.
