interface:
  display_name: "Agent PR Review"
  short_description: "Standardized independent PR review"
  default_prompt: "Use $agent-review-pr-link to review this GitHub PR against its linked issue, run relevant checks, capture relevant UI evidence, and publish the standardized review comment."

policy:
  allow_implicit_invocation: false
